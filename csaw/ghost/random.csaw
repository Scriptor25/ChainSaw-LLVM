@random: num;
@floor: num (x: num);

@randomNum: num (minc: num, mexc: num) {
    ret minc + (mexc - minc) * random();
}

@randomInt: num (minc: num, mexc: num) {
    ret floor(randomNum(minc, mexc));
}
